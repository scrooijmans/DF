# Function expanded_from_single_directory Copy item path

<a href="https://docs.rs/polars-io/0.51.0/x86_64-unknown-linux-gnu/src/polars_io/path_utils/mod.rs.html#145" class="src">Source</a>

``` rust
pub fn expanded_from_single_directory(
    addrs: &[PlPath],
    expanded_addrs: &[PlPath],
) -> bool
```

Available on **crate feature `polars-io`** only.

Expand description

Returns `true` if `expanded_paths` were expanded from a single directory
