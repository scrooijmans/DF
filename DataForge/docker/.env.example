# DataForge Sync Server - Environment Variables
#
# Copy this file to .env and fill in production values
#
# cp .env.example .env

# =============================================================================
# Authentication
# =============================================================================
# Secret key for JWT-like token signing (REQUIRED)
# Generate with: openssl rand -hex 32
SYNC_AUTH_SECRET=your-secret-key-here

# Token expiration in seconds (default: 3600 = 1 hour)
SYNC_AUTH_EXPIRY=3600

# =============================================================================
# Storage Configuration
# =============================================================================
# Backend type: "s3", "minio", or "filesystem"
STORAGE_BACKEND=s3

# S3 endpoint URL (leave empty for AWS S3, set for MinIO/custom S3)
# Examples:
#   AWS S3: (leave empty)
#   MinIO: http://minio:9000
#   DigitalOcean Spaces: https://nyc3.digitaloceanspaces.com
STORAGE_ENDPOINT=

# S3 bucket name or filesystem path (REQUIRED)
STORAGE_BUCKET=your-bucket-name

# S3 region (default: us-east-1)
STORAGE_REGION=us-east-1

# AWS/S3 credentials (REQUIRED for s3/minio backends)
STORAGE_ACCESS_KEY=your-access-key
STORAGE_SECRET_KEY=your-secret-key
