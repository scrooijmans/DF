-- V1__baseline_schema.sql
-- Baseline migration - marker for fresh databases
-- The main SCHEMA constant handles initial table creation
-- This migration exists to establish version tracking baseline

-- For existing databases that are being bootstrapped into Refinery,
-- this migration will be marked as already applied during bootstrap

SELECT 1;
